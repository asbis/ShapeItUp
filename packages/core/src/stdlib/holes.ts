/**
 * Hole library — cut-tool shapes for common mechanical features.
 *
 * Each function returns a Shape3D positioned at the origin pointing -Z.
 * Users cut it from their part: `plate.cut(holes.through("M3").translate(10, 10, 0))`.
 *
 * Implementation: see agent-A worktree.
 */

export {}; // placeholder — agent A fills this module
