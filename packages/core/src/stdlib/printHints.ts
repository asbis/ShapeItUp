/**
 * Print hints — helpers for making shapes that print cleanly on FDM machines.
 *
 * Elephant-foot chamfers, overhang chamfers, first-layer pads.
 * (Teardrop holes live in `holes.ts` since they're a hole variant.)
 *
 * Implementation: see agent-A worktree.
 */

export {}; // placeholder — agent A fills this module
